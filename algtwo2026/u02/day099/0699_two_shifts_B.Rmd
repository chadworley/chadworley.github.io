```{r data generation, echo = FALSE, results = "hide"}
ach = 1:15
while(T){
    xy = sample(6:10,2)
    x = xy[1]
    y = xy[2]
    hk = sample(1:5,2)*sample(c(-1,1),2,T)
    h = hk[1]
    k = hk[2]
    x2 = x-h
    y2 = y+k
    if(length(unique(c(x,y,h,k,x2,y2)))==6){break}
}

eq1 = paste0("y=f(x+",h,")+",k,collapse="")
eq1 = gsub("+-","-",eq1,fixed=T)

al = paste0("* ",ach,collapse = "\n")

```

Question
========

Let function $f$ have an input/output pair such that $f(`r x`)=`r y`$. In other words, the curve $y=f(x)$ contains a point at $(`r x`,`r y`)$.

Based on the information given, you can prove that curve $`r eq1`$ contains point $(x_2,y_2)$.

* $x_2=$ ##ANSWER1##
* $y_2=$ ##ANSWER2##

Answerlist
--------

`r al`
`r al`


Solution
========




Meta-information
============
extype: num
exsolution: 
exname: fa_mc