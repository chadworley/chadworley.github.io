```{r data generation, echo = FALSE, results = "hide"}

qt = 2 #sample(1:2,1)

if(qt==1){
    d = sample(2:9,1)
    woh = sample(4:20,1)
    woh2 = woh+d
    wh = sample(c(woh,woh2))
    w = wh[1]
    h = wh[2]
    if(w<h){
        l1 = w
        l2 = h
        v1 = "w"
        v2 = "h"
        len1 = "width"
        len2 = "height"
    } else {
        l1 = h
        l2 = w
        v1 = "h"
        v2 = "w"
        len1 = "height"
        len2 = "width"
    }
    A = w*h
    P = 2*w+2*h
    eq = paste0(v2,"=",v1,"+",d)
    tx = paste0(len2," is ",d," centimeters **more** than the ",len1,".",collapse="")
}

if(qt==2){
    while(T){
        woh = sample(2:200,1)/10
        rat = sample(11:30,1)/10
        woh2 = woh*rat
        if(woh2%%0.1==0){break}
    }
    wh = sample(c(woh,woh2))
    w = wh[1]
    h = wh[2]
    if(w<h){
        l1 = w
        l2 = h
        v1 = "w"
        v2 = "h"
        len1 = "width"
        len2 = "height"
    } else {
        l1 = h
        l2 = w
        v1 = "h"
        v2 = "w"
        len1 = "height"
        len2 = "width"
    }
    A = w*h
    P = 2*w+2*h
    eq = paste0(v2,"=",rat,v1)
    tx = paste0(len2," is ",rat," times **more** than the ",len1,".",collapse="")
}
wh = c(w,h)
qv = sample(1:2,1)
if(qv==1){
    que_str = paste0("* What is the ",len1,"?",collapse="")
    ans = l1
} else {
    que_str = paste0("* What is the ",len2,"?",collapse="")
    ans = l2
}

qap = sample(1:2,1)
if(qap==1){
    str1 = paste0("A rectangle has an area of ",A," square centimeters.",collapse="")
    ans = l1
} else {
    str1 = paste0("A rectangle has a perimeter of ",P," centimeters.",collapse="")
    ans = l2
}

```


Question
========

`r str1` The `r tx` 

`r que_str`

Solution
========



Meta-information
============
extype: num
exsolution: `r ans`
exname: ticket_prices