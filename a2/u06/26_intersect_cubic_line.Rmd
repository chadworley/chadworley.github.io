```{r,echo=FALSE}
options(scipen=999)
pfix = function(c,d){
    cc = c[c!=0]
    dd = d[c!=0]
    p = paste0("{",cc,"}x^{",dd,"}",collapse="+")
    p = gsub("x^{0}","",p,fixed=T)
    p = gsub("x^{1}","x",p,fixed=T)
    p = gsub("+{-","-{",p,fixed=T)
    p = gsub("+{1}x","+x",p,fixed=T)
    p = gsub("-{1}x","-x",p,fixed=T)
    p = gsub("{-1}x","-x",p,fixed=T)
    p = gsub("{1}x","x",p,fixed=T)
    return(p)
}
mfix = function(c,d){
    cc = c[c!=0]
    dd = d[c!=0]
    p = paste0("+{",cc,"}x^{",dd,"}",collapse="")
    p = gsub("x^{0}","",p,fixed=T)
    p = gsub("x^{1}","x",p,fixed=T)
    p = gsub("+{-","-{",p,fixed=T)
    p = gsub("+{1}x","+x",p,fixed=T)
    p = gsub("-{1}x","-x",p,fixed=T)
    p = gsub("{-1}x","-x",p,fixed=T)
    p = gsub("{1}x","x",p,fixed=T)
    return(p)
}

h = sample(c(-10:-1,1:10),3)
nh = -h
A = 1
B = sum(nh)
C = sum(c(prod(nh[1:2]),prod(nh[c(1,3)]),prod(nh[2:3])))
D = prod(nh)

mb = sample(c(-10:-1,1:10),2)
m = mb[1]
b = mb[2]

ch = c(-10:10)
al = paste0("* ",ch,collapse="\n")

```



Question
========

A cubic polynomial, $p(x)$, and a linear function, $n(x)$, intersect three times. Find the values of $x$ where they intersect.

$$p(x)~=~`r pfix(c(A,B,C+m,D+b),3:0)`$$
$$n(x)~=~`r pfix(c(m,b),1:0)`$$

Answerlist
--------

`r al`


Solution
========

The intersections occur at values of $x$ such that $p(x)=n(x)$.

$$`r pfix(c(A,B,C+m,D+b),3:0)`~=~`r pfix(c(m,b),1:0)`$$

Subtract $`r pfix(c(m,b),1:0)`$ from both sides.


$$`r pfix(c(A,B,C,D),3:0)`~=~0$$


Meta-information
============
extype: num
exsolution: 
exname: cannon_balloon
extol: 0.01